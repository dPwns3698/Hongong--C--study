/* void는 없음 이라는 뜻
C 명령문은 항상 함수들로 이루어진다는 점이 다르다
main이라는 키워드 자체가 프로그램에게 여기부터 시작이라는 표시
명령문 작성시 줄이 넘어가는 부분에는 대부분 세미콜론을 쓴다.
전처리기 지시자, 함수정의, 제어문, 블록을 제외하고는 모두
*/

int main(void)
{
    10 + 20;

    return 0;
}